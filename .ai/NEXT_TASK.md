# Next Allowed Task

Technical Spike 9A (the first deterministic spatial safety oracle,
`spatial-policy-v0.1`, gating `MoveObject`/`ReplaceAsset` before any DCC
launch) is complete and verified; see [STATUS.md](STATUS.md) and
[VALIDATION.md](VALIDATION.md).

## No committed next spike

There is no authorized or provisional next spike at this time. A plausible
future candidate is Technical Spike 9B — Deterministic Walkable Circulation
& Access Graph (pathfinding/circulation is explicitly out of scope for 9A),
but it is NOT authorized and must not be started automatically. Do not
start Spike 9B or any other new spike, renderer, AI integration, download,
or production external `ReplaceAsset` path without explicit user
authorization and a separate scope description.
