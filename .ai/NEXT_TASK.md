# Next Allowed Task

Technical Spike 8J (the first Golden preview rendered entirely from
already-canonical, already-persisted `rev_golden_0012` state — renderer,
CoronaLight, native `AVZ_MATERIAL_*` Corona Physical Materials, and the 28mm
`camera_living_a` all reused, none created, gated by four fresh-process
verification layers) is complete and verified; see [STATUS.md](STATUS.md)
and [VALIDATION.md](VALIDATION.md).

## No committed next spike

There is no authorized or provisional next spike at this time. With 8J
complete, the deterministic DCC foundation (build, revision, camera, and
render layers each independently verified) is now a coherent whole; the next
architecture milestone should be evaluated deliberately rather than assumed.
Do not start any new spike, renderer, AI integration, download, or
production external `ReplaceAsset` path without explicit user authorization
and a separate scope description.
